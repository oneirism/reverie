language: python
cache:
  - "pip"
  - "yarn"
python:
  - "3.6"
install:
  - "nvm install --lts"
  - "nvm use --lts"
  - "pip install coveralls"
  - "pip install -r requirements.txt"
script:
  - "prospector"
  - "yarn install"
  - "yarn build"
  - "yarn test"
after_success:
  - "coveralls"
